-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.5
mypy>=1.10

# Test-only: lets integration tests drive the indexing pipeline directly
# (bypassing the real redis-backed worker) to set up indexed-repository fixtures.
-e ../worker
