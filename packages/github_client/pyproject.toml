[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "codemind-github-client"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "codemind-shared-types",
]

[tool.setuptools.packages.find]
where = ["src"]
